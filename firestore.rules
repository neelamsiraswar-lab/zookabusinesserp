rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all application records across companies, invoices, products, ledgers, etc.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
